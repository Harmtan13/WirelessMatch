<div class="page-spacer">
  <%= link_to "Wireless-Match", root_path%> |
  <%= link_to('Match Quiz', new_user_plan_path) unless session[:form_id] %>
  <%= link_to('Match Quiz', edit_user_plan_path(session[:form_id])) if session[:form_id]%> |
  <%= link_to('Plan Recommendations', plan_recommendations_path) if session[:form_id]%>

  <%= session[:guest_user_id] %>
</div>